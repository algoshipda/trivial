#include <cstdio>
#include <algorithm>
#include <vector>
#define pb(A) push_back((A))
using namespace std;
int n,k;
int num[205];
vector<int> a;
vector<int> b;
int main()
{
        scanf("%d%d",&n,&k);
        for(int i=0;i<n;++i)
                scanf("%d",&num[i]);
        
        a.pb(num[0]);
        for(int i=1;i<n;++i){
                if(a[a.size()-1]!=num[i])
                        a.pb(num[i]);
        }
        
        while(a.size()>1){
                
        
        }
}
